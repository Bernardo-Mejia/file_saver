import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'save_pdf_url',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
